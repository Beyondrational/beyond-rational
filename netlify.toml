# Beyond Rational — static site, no build step.
# Netlify serves the repo root as-is.

[build]
  publish = "."
  command = ""

# Long-cache the design-system assets (filenames are versioned via ?v=).
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=604800"
